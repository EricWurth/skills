{
  "name": "resumebot-browser-plugin",
  "version": "1.1.0",
  "private": true,
  "description": "Chrome extension that fills job applications from a local profile and a growing answer memory, plus a 1Password native messaging host.",
  "license": "MIT",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@1password/sdk": "^0.4.0",
    "jsdom": "^29.1.1",
    "sinon": "^22.1.0"
  }
}
